# VaultMind Secrets
# Copy to .env and fill in your values.
# All non-secret config belongs in config/default.toml — not here.

# --- LLM API Keys (set the one matching your [llm].provider in default.toml) ---

# Anthropic (default provider)
VAULTMIND_ANTHROPIC_API_KEY=sk-ant-...

# OpenAI
# VAULTMIND_OPENAI_API_KEY=sk-...

# Google Gemini
# VAULTMIND_GEMINI_API_KEY=...

# Ollama requires no API key — runs locally.

# --- Embedding API Key ---

# Required if [embedding].provider = "openai" (default)
VAULTMIND_OPENAI_API_KEY=sk-...

# Required if [embedding].provider = "voyage"
# VAULTMIND_VOYAGE_API_KEY=pa-...

# --- Telegram ---
VAULTMIND_TELEGRAM__BOT_TOKEN=123456:ABC-DEF...
# Comma-separated Telegram user IDs allowed to use the bot. Empty = allow all.
VAULTMIND_TELEGRAM__ALLOWED_USER_IDS=[123456789]
